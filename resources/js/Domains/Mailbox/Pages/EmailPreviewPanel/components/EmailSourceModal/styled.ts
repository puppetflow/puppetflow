import styled from 'styled-components';

export const SourceLayout = styled.div`
    display: flex;
    flex-direction: column;
    flex: 1;
    min-height: 0;
`;

export const SourceEditor = styled.div`
    flex: 1;
    min-height: 0;
    overflow: hidden;
`;
