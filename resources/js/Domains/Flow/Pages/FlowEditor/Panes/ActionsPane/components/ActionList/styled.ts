import styled from 'styled-components';
import { ResourceItemInfo } from '@/Domains/Flow/Pages/FlowEditor/Panes/shared.styled';

export const SharedActionInfo = styled(ResourceItemInfo)`
    padding: 6px 0;
`;
