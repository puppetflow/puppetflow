import styled from 'styled-components';

export const TopRow = styled.div`
    display: flex;
    align-items: flex-start;
    gap: 12px;
`;

export const TopRowField = styled.div`
    flex: 1;
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
`;

export const TopRowSwitch = styled.div`
    padding-top: 28px;
    flex-shrink: 0;
`;
