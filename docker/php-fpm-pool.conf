[www]
pm = dynamic
pm.max_children = 20
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 500

; Kill workers stuck longer than 5 min
request_terminate_timeout = 300

; Access log off (nginx handles it)
access.log = /dev/null
