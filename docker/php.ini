; Shared PHP runtime defaults (web, queue workers, artisan) for dev and prod images.
; APP_MEMORY_LIMIT defaults to 512M in the Dockerfile; override it in the container environment.
memory_limit=${APP_MEMORY_LIMIT}
upload_max_filesize=${MEDIA_MAX_UPLOAD_BYTES}
post_max_size=${MEDIA_MAX_REQUEST_BYTES}
